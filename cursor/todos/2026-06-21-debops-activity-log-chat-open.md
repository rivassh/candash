# Open follow-ups — debops activity log (1241c6d0)

<div dir="rtl" style="text-align: right;">

## Pending

- [ ] `monitoring-disk-100gb` — attach disk in ESXi; extend LVM/filesystem on `172.16.1.164` (blocked: no LVM, disk not visible).
- [ ] `zoiper-305-split-tunnel` — fix client OpenVPN split tunnel; credential `654JY&`; confirm REGISTER on `172.16.1.2`.
- [ ] `support-queue-register-302-305` — operators must stay registered on Zoiper/VPN or failover/mobile path applies.
- [ ] `activity-log-full-rows-restore` — optional: restore 18-row `ACTIVITY-LOG-2026-06-13-14.md` if `e8d3564` summary is too coarse.
- [ ] `activity-log-timestamp-crosscheck` — cross-check inferred dates against CDR/Zabbix/git when audit-grade log needed.
- [ ] `debops-split-commit` — many unrelated uncommitted changes; split by topic + secret scan before commit.
- [ ] `activity-log-usage-cost-exact` — import Cursor Usage CSV after `~/.config/cursor-usage.env` is configured.

</div>
