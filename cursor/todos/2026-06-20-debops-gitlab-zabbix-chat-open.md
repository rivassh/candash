# Open TODO � debops GitLab/Zabbix chat � 2026-06-20

**Master list:** [../TODO.md](../TODO.md)

## From this chat only

- [ ] Transfer blocked GitLab projects after cleaning container registry tags:
  - `apps/sana-gps` -> `gps`
  - `apps/tile-server` -> `maps`
  - `apps/cms-site-builder` -> `cms`
  - `apps/shop-app` -> `cms`
- [ ] Resolve physical/network path for printer `172.16.1.141` (device still unreachable by ARP/ICMP).
- [ ] Optional cleanup after transfer validation:
  - `prompts/map` (empty)
  - duplicate candidate `iot/tracker/voip-out` vs `voip/voip-out`
