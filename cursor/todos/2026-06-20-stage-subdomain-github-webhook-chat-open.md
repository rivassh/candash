# Open TODO — stage-subdomain-github-webhook — 2026-06-20

From chat closeout. Merged into [`cursor/TODO.md`](../TODO.md).

## Unfinished from this chat

- [ ] **GitHub webhook Secret** — در GitHub repo webhook همان `GITHUB_WEBHOOK_SECRET` در `/opt/stage-deployer/.env` باشد؛ Redeliver تست
- [ ] **subdomain modular-gps روی stage** — nginx در `stage-router.conf` + `APP_*_HOSTS` در `.env.stage` (یا feature در stage-deployer)
- [ ] **commit/push stage-deployer** — تغییرات `hooks_server.py` + `github_webhook.py` (webhook secret force load + hint)
- [ ] **Rotate Cursor session** — cookie در closeout prompt paste شده
